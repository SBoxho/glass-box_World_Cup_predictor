"""Presentation components for the Streamlit app.

These modules hold *only* UI/presentation logic (Streamlit + HTML/CSS). All modelling and data
access lives in :mod:`core`; components import from ``core`` but never the reverse — the same
one-directional rule the rest of the project follows.
"""
