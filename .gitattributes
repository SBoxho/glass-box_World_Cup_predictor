# Normalize line endings to LF in the repo so CI (Linux) and ruff format --check
# behave identically regardless of the OS a contributor is on.
* text=auto eol=lf

# Treat binary artifacts as binary (no EOL conversion, no diffs).
*.png binary
*.jpg binary
*.joblib binary
*.parquet binary
*.csv binary
